[project]
name = "dog-breed"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "KrisBold12", email = "kristianboldini@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.11.1",
    "onnx>=1.22.0",
    "onnxruntime>=1.28.0",
    "onnxscript>=0.7.1",
    "requests>=2.34.2",
    "scikit-learn>=1.9.0",
    "scipy>=1.18.0",
    "timm>=1.0.28",
    "torch>=2.13.0",
    "torchvision>=0.28.0",
    "tqdm>=4.70.0",
]

[project.scripts]
dog-breed = "dog_breed:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"

[[tool.uv.index]]
name = "pytorch"
url = "https://download.pytorch.org/whl/cu126"
explicit = true

[tool.uv.sources]
torch = { index="pytorch" }
torchvision = { index = "pytorch" }

[dependency-groups]
dev = [
    "pydantic>=2.13",
    "pytest>=9.1.1",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q --strict-markers"
markers = [
    "dataset: needs image data extracted on disk (Stanford ~750 MB, Oxford ~810 MB); skipped when absent",
    "export: needs the exported artifacts in artifacts/; skipped when absent",
    "serving: cross-checks the serving project against this one; skipped when absent",
    "slow: minutes rather than seconds; deselect with -m 'not slow'",
]
